% For embedding math mode macros.
\newcommand{\newcommandm}[2]{\newcommand{#1}{\ensuremath{#2}}}
\newcommand{\renewcommandm}[2]{\renewcommand{#1}{\ensuremath{#2}}}

% this amazing mess comes from page 108 of 'The Latex Companion'
\newcommand{\PreserveBackslash}[1]{\let\temp=\\#1\let\\=\temp}
\let\PBS=\PreserveBackslash\

\newcommandm{\bb}{\beta}
\newcommandm{\bbh}{\beta = 2H - 1}
\newcommandm{\fb}{f^{-\beta}}
\newcommandm{\ff}{1/f}
\newcommandm{\fo}{f^{\,0}}
\newcommandm{\hh}{H}
\newcommandm{\LL}{\mathrm{L}}
\newcommandm{\nf}{N_{\mathrm{f}}}
\newcommandm{\pol}{P_0L}  % have to use o instead of 0 in commands
\newcommandm{\poll}{P_0L^2}  % have to use o instead of 0 in commands
\newcommandm{\po}{P_0}
\newcommandm{\rs}{R/S}
\newcommand{\rsa}{\rs\ analysis}
\newcommandm{\zc}{Z_{\mathrm{crit}}}

%% My original fig directory (any paper that I write can
%% access this directory, cuts down on duplicates, makes sure
%% that if you change it for one paper it is changed in others.
%% \newcommand{\figeps}[1]{/home/ryan/write/fig/eps/#1.eps}
%% For this tutorial, I make the fig directory a subdirectory
%% of the tutorial directory.
\newcommand{\figeps}[1]{fig/#1.eps}

%% To make all plots consistent in height.
%% Make them whatever you want, of course.
\newcommand{\onepicht}{3in}
\newcommand{\twopichtside}{2in}
